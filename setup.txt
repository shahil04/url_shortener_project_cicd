- https://github.com/shahil04/url_shortener_project_cicd.git
- git --> install 
--> go to same file location in cmd (in loction bar CMD )
or open code folder in vs --> cmd type --> code .
git config username and email

--> git init (initilize 1st time in git)
2. git status
3. git add * --> add all file to track
4. git commit -m "notes write "

** github 
	1.login the github
	2.create a repositiory
	3.
git (cmd)--> 1.git remote add origin repositiory
	2. git push -u origin master(your branch name) 

---
update code or version of update in github
1. git status
3. git add * --> add all file to track
4. git commit -m "notes write "
5. git push 

============================================
github Action


==========
docer

- doker hub  push commands 
docker tag url3_jan YOUR_USERNAME/url3_jan:latest
docker tag url3_jan shahil004/url3_jan:latest
docker images
docker push shahil004/url3_jan:latest


- docker hub automations  push automaticlly and publish
